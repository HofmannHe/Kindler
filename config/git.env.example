#!/usr/bin/env bash
# 外部 Git 服务配置示例
# 复制为 config/git.env 并填入真实值

# Git 服务基础 URL（不含仓库路径）
GIT_SERVER_URL=https://git.example.com
# whoami Git 仓库完整地址（ArgoCD 将监听）
GIT_REPO_URL=https://git.example.com/demo/whoami.git
# 访问仓库的账号（公开仓库留空）
GIT_USERNAME=
# 访问仓库的密码或 token（公开仓库留空）
GIT_PASSWORD=

# 可选：默认分支及提交信息
GIT_DEFAULT_BRANCH=main
GIT_SEED_BRANCHES="main develop release master"
GIT_COMMIT_NAME="Kindler Bot"
GIT_COMMIT_EMAIL="kindler@example.com"

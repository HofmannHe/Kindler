#!/usr/bin/env bash
# Argo CD 多仓库配置（每行一个仓库）
# 支持 https / ssh URL；公开仓库无需凭证；私有仓库可配用户名/密码或 token
# 示例：
# ARGOCD_REPOS<<EOF
# https://github.com/argoproj/argocd-example-apps.git
# https://github.com/hofmannhe/some-private-repo.git
# EOF

# 默认仅包含当前仓库（可按需追加）。为空将被脚本忽略。
ARGOCD_REPOS=$(cat <<'EOF'
${GIT_URL:-}
EOF
)

# 初始管理员密码（仅用于本地快速环境）
ARGOCD_ADMIN_PASSWORD=admin123
